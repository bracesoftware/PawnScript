# Using PawnScript from your Pawn script

```pawn
CallRemoteFunction("pawnscriptcall", "s", "using.console");
CallRemoteFunction("pawnscriptcall", "s", "console.println.log("PawnScript from Pawn")");
```